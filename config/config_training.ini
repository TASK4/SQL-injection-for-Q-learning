; [Target]
; # Mock cho training, không dùng real URL
; search_url = http://mock-juice-shop.local/rest/products/search
; search_param = GET

; [Environment]
; normal_result_count = 9
; success_marker = admin@juice-sh.op
; sql_error_marker = SQLITE_ERROR

[Agent]
learning_rate = 0.1
discount_factor = 0.9
epsilon = 1.0
epsilon_decay = 0.995
epsilon_min = 0.05

[Training]
# Mock-specific
success_marker = admin@juice-sh.op
sql_error_marker = SQLITE_ERROR
baseline_enabled = True

# Training params
episodes = 5000
# (Đã giảm từ 50k xuống 5k vì logic reward mới học nhanh hơn)

max_steps_per_episode = 30
# (Giảm số bước tối đa để tránh lặp vô tận)



